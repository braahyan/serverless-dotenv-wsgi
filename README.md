# serverless-dotenv-wsgi
small project demonstrating the use of serverless, the serverless-wsgi plugin, and the serverless-plugin-dotenv plugin.

Install:
* Serverless 1+
* Python 2.7
* VirtualEnv

Setup:
* npm install
* sls deploy/sls wsgi serve
